deploy:
  variables:
    - name: RUN_MIGRATIONS
      title: Run database migrations on deploy
      default: 1
ci:
  require:
    - ci/travis
tasks:
  info:
    action: "Info"
    description: "Some information that needs to be displayed"
    steps:
      - hostname
review:
  checks:
    - bundle exec rake db:migrate:status
